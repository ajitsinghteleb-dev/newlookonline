{
  "entities": {
    "NewsArticle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NewsArticle",
      "type": "object",
      "description": "Represents a news article fetched and processed by the system.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the news article."
        },
        "headline_hi": {
          "type": "string",
          "description": "The title of the news article translated into Hindi."
        },
        "summary": {
          "type": "string",
          "description": "A short summary of the news article."
        },
        "summary_hi": {
          "type": "string",
          "description": "A short summary of the news article in Hindi."
        },
        "category": {
          "type": "string",
          "description": "The category of the news article (e.g., World, Business, Tech)."
        },
        "region": {
          "type": "string",
          "description": "The region the news article pertains to (e.g., US, IN, Global)."
        },
        "link": {
          "type": "string",
          "description": "URL to the original source of the news article."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the news article was published.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "enum": [
            "published",
            "draft"
          ],
          "description": "The publication status of the article."
        },
        "credibilityScore": {
            "type": "number",
            "description": "A score from 0.0 to 1.0 indicating the perceived credibility of the news source/content."
        }
      },
      "required": [
        "title",
        "summary",
        "category",
        "region",
        "link",
        "timestamp",
        "status",
        "credibilityScore"
      ]
    },
    "JobPosting": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "JobPosting",
      "type": "object",
      "description": "Represents a job posting scraped from various job APIs.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the job posting."
        },
        "company": {
          "type": "string",
          "description": "The company offering the job."
        },
        "source": {
          "type": "string",
          "description": "The source of the job posting (e.g., External, Internal)."
        },
        "link": {
          "type": "string",
          "description": "URL to the original job posting."
        },
        "posted_at": {
          "type": "string",
          "description": "The date the job was posted.",
          "format": "date-time"
        },
        "is_active": {
          "type": "boolean",
          "description": "Whether the job posting is currently active."
        },
        "skills": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "A list of required skills for the job."
        }
      },
      "required": [
        "title",
        "company",
        "source",
        "link",
        "posted_at",
        "is_active"
      ]
    },
    "Tender": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Tender",
      "type": "object",
      "description": "Represents a tender opportunity scraped from open tender portals.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the tender."
        },
        "organization": {
          "type": "string",
          "description": "The organization issuing the tender."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the tender requirements and scope."
        },
        "url": {
          "type": "string",
          "description": "URL to the original tender document."
        },
        "closingDate": {
          "type": "string",
          "description": "The closing date for submitting the tender.",
          "format": "date-time"
        },
        "tenderValue": {
            "type": "string",
            "description": "The estimated value of the tender."
        }
      },
      "required": [
        "title",
        "organization",
        "description",
        "url",
        "closingDate"
      ]
    },
    "Ad": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Ad",
      "type": "object",
      "description": "Represents an advertisement request submitted by a user.",
      "properties": {
        "businessName": {
          "type": "string",
          "description": "The name of the business submitting the ad."
        },
        "paymentUTR": {
          "type": "string",
          "description": "The payment transaction ID (UTR) for the ad placement."
        },
        "bannerUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL of the uploaded ad banner image."
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "active",
            "rejected"
          ],
          "description": "The current status of the ad request."
        },
        "submittedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the ad was submitted."
        }
      },
      "required": [
        "businessName",
        "paymentUTR",
        "bannerUrl",
        "status",
        "submittedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/news/{newsId}": {
      "schema": {
        "$ref": "#/entities/NewsArticle"
      },
      "description": "Stores news articles. Includes fields like 'title', 'headline_hi', 'summary', 'category', 'region', 'link', and 'timestamp'."
    },
    "/jobs/{jobId}": {
      "schema": {
        "$ref": "#/entities/JobPosting"
      },
      "description": "Stores job postings. Includes fields like 'title', 'company', 'source', 'link', 'posted_at', and 'is_active'."
    },
    "/tenders/{tenderId}": {
      "schema": {
        "$ref": "#/entities/Tender"
      },
      "description": "Stores tenders. Includes fields like 'title', 'organization', 'description', 'url', and 'closingDate'."
    },
    "/ads/{adId}": {
      "schema": {
        "$ref": "#/entities/Ad"
      },
      "description": "Stores advertisement requests. Includes 'businessName', 'paymentUTR', 'status', and 'submittedAt'. Writable by admins only."
    }
  }
}
